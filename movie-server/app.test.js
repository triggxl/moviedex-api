const supertest = require('supertest');
const app = require('./app');
const { expect } = require('chai');

describe('', () => {
  it('should return a 400 if input is invalid', ()=> {
    
  })
  it('', ()=> {
    
  })
  it('', ()=> {
    
  })
  it('', ()=> {
    
  })
})